import { ModePlaceholder } from '@/components/ModePlaceholder';

export default function OperationsScreen() {
  return (
    <ModePlaceholder
      title="Operations view"
      description="The guest view is built. The operations view comes next."
    />
  );
}
