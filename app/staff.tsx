import { ModePlaceholder } from '@/components/ModePlaceholder';

export default function StaffScreen() {
  return (
    <ModePlaceholder
      title="Staff view"
      description="The guest view is built. The staff view comes next."
    />
  );
}
